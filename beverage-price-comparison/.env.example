# 数据快照定时刷新间隔（秒）。0=关闭（开发默认）；生产建议 300
REFRESH_INTERVAL_S=0

# 每客户端每分钟请求上限。0=不限流（开发默认）；生产建议 120
RATE_LIMIT_PER_MIN=0

# CORS 允许来源，逗号分隔。生产按需收紧，如 https://your-domain.com
CORS_ORIGINS=*

# /admin/refresh 管理令牌。为空=不校验（仅限开发）；生产必须设置
ADMIN_TOKEN=
